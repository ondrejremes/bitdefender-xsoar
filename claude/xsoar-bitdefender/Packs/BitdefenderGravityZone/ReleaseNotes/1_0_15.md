#### Integrations
##### Bitdefender GravityZone API
- **bd-endpoint-list**: fixed `is_managed` filter — now correctly uses the nested `filters.security.management` structure required by the API (previously sent as a top-level `isManaged` parameter which was silently ignored).
