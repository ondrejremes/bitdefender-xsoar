#### Integrations
##### Bitdefender GravityZone API
- **bd-endpoint-list**: changed defaults to `is_managed=true`, `per_page=1000`, `page=1` — calling without arguments now returns all managed endpoints.
