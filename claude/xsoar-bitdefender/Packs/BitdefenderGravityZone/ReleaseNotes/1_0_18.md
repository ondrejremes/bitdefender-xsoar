#### Integrations
##### Bitdefender GravityZone API
- Fixed API version routing: default is now v1.0 for all calls. Only `getEndpointsList` explicitly uses v1.1 (where `isManaged` filter is supported). All other methods, including push API, correctly use v1.0.
