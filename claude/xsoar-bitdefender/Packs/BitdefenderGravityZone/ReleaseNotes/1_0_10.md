#### Integrations
##### Bitdefender GravityZone API
- Fixed **bd-push-test** and all boolean-result commands: any non-False API response is now treated as success (the API raises an exception for actual errors; a dict response is valid success data).

#### Classifiers
##### Bitdefender GravityZone - Incoming Mapper
- Fixed EDR/XDR Incident field names based on real push event data:
  - `severityScore` (was `severity_score`)
  - `process_path` (was `detection_path`)
  - `parent_process_path` (was `detection_parentPath`)
  - `severity` mapped directly as string (low/medium/high) instead of numeric range conversion
  - Added `file_hash_sha256` → GravityZone SHA256
- Added `protocol_id` → **GravityZone Protocol** mapping for Network Events (firewall blocked connections)

#### Incident Fields
- Added **GravityZone Protocol** field — network protocol ID of blocked connection (6=TCP, 17=UDP)
