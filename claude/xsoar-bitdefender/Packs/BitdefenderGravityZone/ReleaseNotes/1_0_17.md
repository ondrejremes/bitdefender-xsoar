#### Integrations
##### Bitdefender GravityZone API
- Fixed push notification commands broken by v1.1 upgrade: push API methods (`getPushEventSettings`, `setPushEventSettings`, `sendTestPushEvent`) only exist in v1.0 and now explicitly use that version.
