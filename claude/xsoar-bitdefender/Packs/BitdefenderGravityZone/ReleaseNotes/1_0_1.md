#### Integrations
##### Bitdefender GravityZone API
- Fixed **test-module** — uses `getEndpointsList` instead of `getLicenseInfo` (getLicenseInfo is only available for partner/reseller API keys).
- Fixed URL normalization — integration now works correctly whether the configured URL ends with `/api` or not.
