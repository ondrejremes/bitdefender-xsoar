#### Integrations
##### Bitdefender GravityZone API
- Fixed **bd-quarantine-list**, **bd-quarantine-item-remove**, **bd-quarantine-item-restore** — service name (`computers`/`exchange`) is now correctly passed as part of the URL path (`/api/v1.0/jsonrpc/quarantine/{service}`) instead of as a JSON parameter.
- Added `service` parameter to **bd-quarantine-item-restore** (was missing, now required — same as the other quarantine commands).
