#### Integrations
##### Bitdefender GravityZone API
- **bd-push-settings-set**: added early validation — command now fails with a clear message if the webhook URL uses plain HTTP instead of HTTPS. GravityZone requires TLS 1.2+ on the target webhook server.
